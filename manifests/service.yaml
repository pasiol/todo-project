apiVersion: v1
kind: Service
metadata:
  name:
    todo-project-frontend-svc
  namespace: todo-project
spec:
  type: LoadBalancer
  selector:
    app: todo-project-frontend
  ports:
    - port: 80
      protocol: TCP
      targetPort: 3000